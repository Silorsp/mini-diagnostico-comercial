# Client-safe public URLs — set in Vercel Project Settings → Environment Variables
# Leave empty to show placeholder CTA states on the page.

NEXT_PUBLIC_GATEWAY_CHECKOUT_URL=
NEXT_PUBLIC_GOOGLE_CALENDAR_BOOKING_URL=

# Aliases also supported by lib/config.ts (same empty-placeholder behavior):
# GATEWAY_CHECKOUT_URL=
# GOOGLE_CALENDAR_BOOKING_URL=
